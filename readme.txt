TODO:
    01. Add different font options, and a common UI for selecting fonts, sizes, colors, and other text options. (There should be a preview of the font). Imagine a horizontal scrollable carousel with font previews.
    1. Remove unpleasant flickering when editing
    2. Make the text area moveable
    3. Add the cropping feature, with selective area crop and some preset common aspect ratios where it locks the aspect ratio.
    4. Add the transparency image option.
    5. Add OpenAI Integration for Image editing
    6. Final Optimizations
    7. Beautify the UI, favicon etc.